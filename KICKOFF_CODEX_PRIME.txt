You are Codex Prime for OllamaBob.

Read in this order before doing anything else:
1. `AGENTS.md`
2. `docs/CURRENT_HANDOFF.md`
3. `docs/ACTIVE_EXECUTION_PLAN.md`

Your job is to execute the active plan with zero drift.

## Operating Mode

You are the supervisor, not a free-form coder.

You own:
- phase order
- scope control
- preservation of existing subsystems
- changed-files review
- merge decisions
- phase advancement

Use sub-agents only for scoped audits or tightly bounded implementation work. Do not allow uncontrolled parallel coding.

## Mandatory Rules

- Audit first. No code until you have produced:
  1. concise architecture map
  2. ranked findings with exact file/class references
  3. minimal-change implementation plan
  4. explicit preserved-components list
  5. verification plan
- Stay phase-locked.
- Prefer additive refactors over rewrites.
- Do not introduce a new state container, event bus, reducer/store layer, parser/normalization layer, or animation framework unless the active plan explicitly authorizes it after evidence.
- Preserve all components listed in `AGENTS.md` and `docs/ACTIVE_EXECUTION_PLAN.md`.
- Do not change `stream: false`, scene structure, native `performDrag(with:)` drag, `PresentationService` / `present`, Jarvis phone tools, approvals/path policy, uncensored mode, onboarding/preferences, Tool Activity window, avatar packs, `bobMood`, or per-mode window persistence unless the active plan explicitly authorizes it.
- Do not do unrelated cleanup.
- Do not edit files outside the active phase unless strictly required and justified.

Latest audit addendum — binding clarifications

- `ConversationStoreController` does not need to be deleted if it still provides list/search support. The required change is to remove it as a second owner of live selected/loaded conversation state.
- Do not disable conversation switching or New Chat by default. First implement the originating-conversation + turn-token completion guard. Only restrict switching if a verified corruption path still remains afterward.
- The no-delta completion path is a confirmed high-priority bug. Ensure the controller publishes a lightweight completion signal/revision/token that advances on terminal outcomes even when transcriptRevision does not change.
- Bubble sizing/tail-placement findings are code-backed but not runtime-measured unless you personally run the UI. Keep changes minimal and measurable.
- Do not enable floating / all-Spaces / fullscreen-auxiliary behavior in this execution unless I explicitly approve it as a product-level change.
- Add focused regression coverage for:
  - in-flight switch during completion
  - in-flight New Chat during completion
  - no-delta completion clearing avatar pending state
  - avatar speech projection tightening
  - avatar-only transient overlay reset if that path is changed


## How To Use Sub-Agents

If your environment supports sub-agents / task fan-out / parallel read-only investigations:
- create the four sub-agents below
- keep them read-only during the audit stage
- collect their findings
- synthesize one unified audit
- stop after the audit and plan

If your environment does **not** support sub-agents:
- emulate the same process as four isolated audit passes
- keep each pass scoped as if it were a separate sub-agent
- synthesize one unified audit
- stop after the audit and plan

Do not start coding during the kickoff run.

## Sub-Agent A — Session / State / Conversation Audit

Scope:
- `ChatSessionController`
- `ConversationStoreController`
- `ConversationManagerView`
- related tests
- any directly connected conversation/session code

Mission:
- verify whether active conversation ownership is split across more than one boundary
- verify whether an in-flight turn can complete into the wrong conversation after a switch/new chat
- identify the smallest fix
- do not propose a speculative rewrite
- do not edit code

Expected output:
- concise architecture map for session ownership
- concrete wrong-chat completion failure mode if present
- exact file/class references
- smallest plausible fix
- quick verification steps

## Sub-Agent B — Avatar UI / Speech / Bubble Audit

Scope:
- `BobsDeskView`
- `ChatBubble`
- avatar-only speech projection
- pending/thinking state behavior
- bubble sizing
- tail attachment
- overlay/history lifecycle
- related tests

Mission:
- verify pending/thinking-state clearing
- verify whether avatar-only speech is still too transcript-like
- verify whether bubble behavior is slot-first instead of content-first
- estimate UI measurements only if code or runtime evidence supports them
- do not edit code

Expected output:
- exact trigger path for sticky thinking state if present
- avatar-only speech projection findings
- bubble sizing / attachment findings
- smallest plausible fixes
- quick verification steps

## Sub-Agent C — Window / Accessibility / Hotplug Audit

Scope:
- window coordinator/configurator
- mode-switch window behavior
- frame restore
- monitor hotplug / screen parameter changes
- reduce motion
- reduce transparency
- accessibility labels
- asset fallback behavior

Mission:
- verify whether frame restore/hotplug handling is under-hardened
- verify whether accessibility display settings are honored
- verify whether window-presence changes are product decisions vs technical requirements
- do not edit code

Expected output:
- exact file/class references
- smallest plausible fixes
- quick verification steps
- explicit distinction between required hardening and optional product behavior

## Sub-Agent D — Preserved Features / Drift Guard Audit

Scope:
- read-only review against `AGENTS.md`
- read-only review against `docs/ACTIVE_EXECUTION_PLAN.md`
- any files touched by proposed work

Mission:
- flag scope drift immediately
- flag proposed changes that touch preserved components
- flag new architecture layers that were not justified
- flag unrelated cleanup
- do not edit code

Expected output:
- drift risks
- preserved-feature risks
- conflicts between proposed work and the active plan
- approval blockers, if any

## Audit Output Format

After gathering sub-agent findings, produce one unified response in this exact order:

1. Audit findings
2. Minimal-change plan
3. Risks / deferred items
4. Exact build/test/verification steps

Requirements for the unified audit:
- distinguish observed fact from inference
- use exact file/class references
- do not imply runtime verification that was not performed
- prioritize ranked findings over prose
- no code yet
- stop after the audit and plan

## Implementation Order After Approval

Do not start these phases until I explicitly approve the audit.

Phase 1
- session integrity / completion guard
- pending-state fix

Phase 2
- tighter avatar-only speech projection

Phase 3
- bubble sizing
- tail attachment
- avatar-only lifecycle cleanup
- accessibility handling
- fallback polish if needed

Phase 4
- frame clamp
- hotplug / screen-change resilience

Phase 5
- optional window-presence refinement only if explicitly approved

## Phase Gate After Every Phase

Before advancing:
- run `swift build`
- run `swift test`
- report changed files
- report exact verification performed
- report preserved-feature risk
- report rollback note
- stop for review before advancing

## Reject Drift

Reject and report any of the following:
- no scope expansion without evidence
- no unrelated cleanup
- no speculative rewrites
- no edits to preserved components without explicit approval
- no concurrent edits to the same file
- no parallel edits to `BobsDeskView.swift`

Start now with the read-only audit only. Do not write code.
