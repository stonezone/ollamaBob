Phase 1 is accepted.

Continue in this same session and execute Phase 2 only.

Do not redo the broad audit.
Do only a narrow spot-check of the files you are about to touch to confirm nothing materially changed.
If you find a contradiction, stop and report it before coding.

Phase 2 goal:
Tighten avatar-only speech projection so Avatar Mode feels like spoken output, not a mini transcript.

Binding constraints:
- Keep the current architecture.
- No new parser.
- No new normalization layer.
- No second message model.
- No Full-mode transcript changes.
- No bubble geometry/tail work yet.
- No accessibility/window/hotplug work yet.
- No cleanup outside the direct Phase 2 scope.
- Do not revisit Phase 1 logic unless strictly required for compilation/test stability.

Primary target:
- OllamaBob/OllamaBob/Views/ChatBubble.swift

Secondary target only if strictly necessary:
- related tests in OllamaBob/Tests/OllamaBobTests/ChatBubbleRenderingTests.swift

What to do:
1. Narrowly spot-check the current avatarBubblePreview path and confirm the smallest safe in-place change.
2. Tighten avatar-only projection heuristics so avatar speech becomes short, speech-like, and low-noise.
3. Preserve existing Full-mode rendering.
4. Preserve any existing rich-view / HTML / artifact placeholder behavior unless there is a direct bug.
5. Add or update focused tests for:
   - long prose
   - prose + list
   - prose + code
   - tool-heavy reply
   - confirmation that Full mode remains unchanged while avatar-only projection tightens

Desired behavior:
- avatar-only replies should usually collapse to 1–3 short spoken lines
- code-heavy content should not render like a code transcript in the primary avatar speech surface
- list-heavy/tool-heavy content should be collapsed harder into concise spoken output
- avoid developer-noise leakage in avatar mode
- preserve semantic meaning; do not make the avatar preview misleading

Preserved components still untouched:
- AgentLoop -> ChatSessionController -> BobsDeskView
- stream: false
- PresentationService / present
- Jarvis phone tools
- approvals/path policy
- uncensored mode
- onboarding/preferences
- Tool Activity window
- avatar packs + bobMood
- native performDrag(with:)
- per-mode frame persistence
- current scene structure
- rich HTML companion window
- Phase 1 session-integrity and terminal-completion changes

Required workflow:
1. Give a concise Phase 2 execution plan before coding:
   - files to edit
   - exact heuristics you plan to tighten
   - tests to add/update
   - preserved items explicitly untouched
2. Implement Phase 2 only.
3. Run:
   - cd /Users/zack/ollamaBob/OllamaBob
   - swift build
   - swift test
4. Report:
   - changed files
   - why each changed
   - test/build results
   - remaining Phase 2 risk
   - exact manual verification steps
5. Stop and wait for approval before Phase 3.

Required manual verification to include:
- In Avatar Mode, test:
  - long prose reply
  - prose + list
  - prose + code
  - tool-heavy reply
- Confirm avatar speech is more concise and speech-like.
- Confirm Full mode transcript rendering is unchanged.
- Confirm no Phase 1 behavior regressed.

Output format:
1. Narrow spot-check summary
2. Phase 2 execution plan
3. Code changes made
4. Test/build results
5. Manual verification checklist
6. Deferred items for Phase 3

Do not start Phase 3.
Do not widen scope.
Stop after Phase 2.